FROM node:20-slim
RUN npm install -g pnpm@9
WORKDIR /app
COPY . .
RUN pnpm install --frozen-lockfile
RUN pnpm --filter @workspace/api-server run build
CMD ["sh", "-c", "cd /app/artifacts/api-server && node --enable-source-maps ./dist/index.mjs"]
