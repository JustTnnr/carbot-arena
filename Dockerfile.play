FROM node:20-alpine
WORKDIR /app
RUN npm install -g pnpm
COPY . .
RUN pnpm install --frozen-lockfile
RUN pnpm --filter @workspace/play run build
RUN npm install -g serve
CMD ["sh", "-c", "serve artifacts/play/dist -l $PORT"]
