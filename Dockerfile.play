FROM node:20-slim
RUN npm install -g pnpm@9
WORKDIR /app
COPY . .
RUN pnpm install --frozen-lockfile
RUN pnpm --filter @workspace/play run build
CMD ["sh", "-c", "cd /app/artifacts/play && node_modules/.bin/vite preview --host 0.0.0.0 --port ${PORT:-3000}"]
