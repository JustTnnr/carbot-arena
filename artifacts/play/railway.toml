[build]
builder = "nixpacks"
buildCommand = "pnpm install --frozen-lockfile && pnpm --filter @workspace/play run build"

[deploy]
startCommand = "cd /app/artifacts/play && node_modules/.bin/vite preview --host 0.0.0.0 --port $PORT"
restartPolicyType = "always"
